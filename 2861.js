var input = require('fs').readFileSync('/dev/stdin', 'utf8');
var lines = input.split('\n');
var C = Number(lines.shift())
if(2 <= C <= 99){
    for(var d = 0;d < C; d++){
        var d = lines.shift()}
    for(var e = 0;e < C; e++){
        console.log('gzuz')}}
